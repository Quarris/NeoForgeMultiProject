pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'multiproject'

// Add each mod's folder name here (see README "Adding a mod").
// include('core', 'mymod')
